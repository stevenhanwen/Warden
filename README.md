# Warden

A small macOS command-line tool that scans running processes and flags ones using more memory than a configured limit.

> Work in progress.

## Build and run

```sh
make
make run
make clean
```
